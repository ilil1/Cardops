# BankChurners 기반 구현의 한계와 데이터 전환 배경

BankChurners는 **신용카드 고객 10,127명의 정보를 고객별 한 행으로 정리한 정적 데이터**입니다. 연령·소득·카드 특성, 최근 12개월 거래건수·금액 등의 이용 통계와 이탈 여부(`Attrition_Flag`)를 포함합니다.

**날짜별 거래 이력과 고객 특성의 관측일, 실제 이탈일은 제공하지 않습니다.** 따라서 입력 정보와 이후 결과의 시간 관계를 확인할 수 없어, 미래 특정 기간의 이탈이나 다음 달 거래량을 예측하는 성능을 이 데이터만으로 검증하기 어렵습니다.

## 1. 기존 데이터와 분석 결과의 해석 범위

### 1.1 분류 모델은 이미 기록된 이탈 고객의 특성을 구분한다

`BankChurners.csv`는 고객별 한 행으로 된 정적 데이터입니다. 고객 특성이 **언제 관측됐는지**, 그 뒤 **언제 이탈했는지**를 연결할 월별 기록이 없습니다. 분류 모델도 고객 행을 무작위로 나눠 평가하므로, 높은 점수는 이 데이터에서 기록된 이탈·비이탈 고객을 잘 구분했다는 의미입니다.

따라서 이 모델을 월별 데이터를 받아 “다음 달이나 3개월 안에 이탈할 고객”을 미리 찾는 시뮬레이션으로 해석할 근거는 부족합니다. 모델이 높은 위험 점수를 부여한 고객은 데이터에 기록된 이탈 고객과 비슷한 특성을 가진 고객으로 해석하는 것이 적절합니다.

현재 모델은 프로토타입에서 “**고객 프로필을 바탕으로 위험 점수를 계산하는 흐름**”을 보여줄 수 있습니다. 그러나 “**월별 변화로 미래 이탈을 조기 경보한다**”고 표현하면 실제 구현과 검증 범위를 넘어섭니다.

근거: [분류 모델의 데이터 분할](../../src/final/classification_final.py) · [BankChurners 원본](../../data/raw/BankChurners.csv)

### 1.2 이미 이탈한 고객이 유지 캠페인 후보처럼 보일 수 있다

원본에는 `Attrition_Flag`로 이탈 여부가 기록되어 있지만, 기존 고객 DB 적재 필드에는 이 정보가 포함되지 않습니다. 이후 분석 배치는 적재된 고객을 대상으로 점수를 계산합니다. 이 과정에서 이미 이탈한 고객과 아직 거래 중인 고객을 캠페인 자격 기준으로 구별하지 못할 수 있습니다.

기존 분석 기록에서는 원본과 모델 산출물을 대조했을 때 **고위험 점수를 받은 1,579명 중 1,549명이 원본에서 이미 이탈 고객**이었습니다.

> 이 수치는 당시 원본 데이터에 모델을 적용한 분석 기록입니다. 현재 DB 인원수나 새 Synchrony 모델의 평가 결과가 아니며, 이번 문서 정리에서 다시 집계한 값도 아닙니다.

가상 회사의 캠페인 시나리오에서도 이탈 고객과 아직 거래 중인 고객을 구별하지 않으면 이탈 방지 캠페인의 대상이 부정확해집니다. 원본의 이탈 여부는 모델 입력값으로 쓰지 않더라도, **시연 대상이 아직 거래 중인 고객인지 구분하는 데** 활용할 필요가 있습니다.

새 데이터로 전환할 때에도 예측 기준일에 이미 해지한 고객을 제외하는 규칙을 모델 실험과 실제 캠페인 대상 선정에 모두 반영해야 합니다.

근거: [고객 데이터 적재 필드](../../backend/ai-service/cardops_ai/app/customer_import.py) · [분석 대상 점수화](../../backend/ai-service/cardops_ai/app/analysis_batch.py)

### 1.3 회귀 모델의 활동성 갭 계산 방식과 해석 범위

기존 회귀 모델은 고객 특성으로 데이터에 담긴 **최근 12개월 거래건수**를 추정하고, 같은 구간의 실제 거래건수와 비교해 활동성 갭을 계산합니다. 구현에는 이번 달과 지난달의 거래건수를 비교하는 계산이 없습니다. **고객 특성에 따른 예상 대비 거래량 차이를 분석하는 지표**로 설계되어 있습니다.

```text
활동성 갭 = 실제 거래건수 − 모델이 예상한 거래건수
```

갭이 음수이면 **같은 12개월 구간에서 모델 예상보다 실제 거래가 적었다**는 뜻입니다. 예를 들어 예상 80건, 실제 50건이면 갭은 −30건입니다. 이 계산에는 지난달 거래건수가 들어가지 않습니다.

갭 하위 20%를 검토 대상으로 정하는 것은 사람이 설정한 상대적 선정 규칙이며, 고객 관리에 활용할 수 있는 설계입니다. 하위 20%에 속하는 것과 갭이 음수인 것은 서로 다른 조건입니다. 기존 학습 코드에는 갭과 기록된 이탈 여부의 상관, AUC, 하위 20% 포착률 등을 평가하는 절차도 있습니다. 이 평가는 정적 데이터에 기록된 이탈 상태와 갭의 관계를 확인하며, 미래 특정 기간의 이탈 예측력을 검증하는 데에는 시간순 관측과 별도 평가가 필요합니다.

현재 확인되는 표현상의 문제는 캠페인 화면의 ‘중위험·활동성 하락 재활성화’와 갭 기반 군집 이름의 ‘우선케어(거래 감소)’입니다. 이 문구들은 시간에 따른 감소를 연상시킬 수 있으므로, 갭을 설명할 때는 “**모델 예상 대비 거래량 차이**”라는 계산 의미에 맞추는 것이 정확합니다. 월별 하락이나 다음 달 거래량 예측은 새로운 시계열 데이터에서 별도로 구현·검증할 확장 기능입니다.

근거: [회귀 목표값 정의](../../src/feature_engine/regression_features.py) · [활동성 갭 계산·군집 이름](../../backend/ai-service/cardops_ai/app/analysis_batch.py) · [갭과 이탈 상태의 평가 절차](../../src/final/regression_final.py) · [캠페인 화면의 세그먼트 이름](../../frontend/src/api/campaigns.ts)

## 2. 이 한계를 개선하기 위한 새로운 작업

CardOps의 목표는 **카드를 아직 해지하지 않은 고객의 과거 이용 기록으로 향후 해지 위험을 예측하고, 이후 관측된 결과로 모델을 평가하는 것**입니다. 이를 위해 고객 특성의 관측 시점, 예측 기준일, 실제 해지일을 연결할 수 있는 데이터 구조가 필요합니다.

날짜별 거래와 카드 발급·해지일이 있는 Synchrony 데이터로 기준일별 입력·미래 정답 생성과 분류 모델의 시간순 평가를 오프라인 실험으로 구현했습니다. 기존 로그인·권한·고객 조회·캠페인 이력 등의 서비스 기반은 재사용하며, 새 고객·거래 구조와 모델 결과를 서비스 DB·API·화면에 연결하는 작업은 후속 개발 범위입니다.

### 2.1 기존 구현·데이터와 확장 방향

| 기존 구현·데이터의 범위 | 개선·확장 방향 | 현재 진행 상태 |
| --- | --- | --- |
| 고객별 정적 특성으로 기록된 이탈·비이탈 상태를 분류 | 기준일까지의 거래로 이후 30일·60일 해지를 예측 | 입력·정답 생성과 분류 모델 비교 구현 |
| 기존 고객 DB에 원본 이탈 상태를 보존하지 않아, 유지 대상 선정 시 이미 이탈한 고객을 구분하지 못함 | 기준일에 발급되어 있고 아직 해지하지 않은 고객만 선정 | 실험의 대상 필터에 적용. 서비스 DB·타기팅 연결은 남아 있음 |
| 같은 기간의 활동성 갭에 ‘활동성 하락’ 등 시간 변화로 읽힐 수 있는 표현 사용 | 갭의 계산 의미를 명시하고, 기간별 이용 변화와 다음 달 거래량 예측은 별도로 구성 | 변화 특징과 다음 달 거래건수·금액 정답 생성 구현. 새로운 회귀 모델 학습·평가는 후속 범위 |
| 분석 기준일별 스냅샷과 결과 이력 관리 구현. BankChurners 기반 월별 MLOps는 구축하지 않음 | 새 데이터의 거래 발생일로 상태를 구성하고 결과 관측 이후 학습·평가 | Synchrony 오프라인 실험에 월별 스냅샷·시간순 재학습 비교·동결 모델 평가 구현. 자동 운영 파이프라인은 후속 범위 |

### 2.2 예측 기준일과 미래 정답의 예시

**2026-03-31이 끝난 시점에 향후 30일 내 해지를 예측**한다면 다음과 같이 구성합니다.

1. **대상 고객**: 3월 31일까지 카드가 발급되었고 그날까지 해지하지 않은 고객입니다.
2. **모델 입력**: 3월 31일까지 발생한 거래로 최근 30·90일 거래건수·금액, 마지막 구매 후 경과일, 이전 기간 대비 변화를 계산합니다. 4월 거래나 미래 해지일은 입력에 넣지 않습니다.
3. **평가 정답**: 이후 기록으로 4월 1일부터 4월 30일까지 해지했는지를 확인합니다. 전체 결과 관측 기간이 부족한 표본을 임의로 ‘미해지’로 채우지 않습니다.
4. **월별 분류기 학습**: 과거 기준일의 표본도 그 예측 기간의 결과가 모두 관측된 뒤 학습에 사용합니다.

입력과 정답 생성은 [월별 스냅샷 코드](../../src/experiments/synchrony/data.py), 학습 규칙은 [학습·평가 분리 점검](../synchrony_split_audit.md)에 정리되어 있습니다.

### 2.3 현재 구현 상태와 후속 작업

현재는 **BankChurners 서비스 기반을 재사용하면서 시간순 해지 예측 구조로 전환하는 단계**입니다. Synchrony의 월별 스냅샷, 30일·60일 해지 정답 생성, 분류 모델 비교, 시간순 재학습 비교와 동결 모델 평가는 오프라인 실험으로 구현했습니다. 새 모델의 결과를 기존 API·화면·캠페인에 연결하는 작업은 남아 있습니다.

#### 서비스 전환과 운영 자동화

- 새 고객·거래 구조의 DB 적재와 API·화면 연결
- 예측 기준일의 카드 발급·해지 상태를 서비스 DB와 캠페인 대상 선정에 반영
- 다음 달 거래량 회귀와 행동 군집의 학습·평가
- 월별 자동 데이터 처리, 재학습, 모델 승격·롤백

기존 캠페인 서비스에는 마케팅 수신 거부, 최근 접촉, 중복 등록 등의 자격 검증이 구현되어 있습니다. 새 데이터로 전환할 때는 여기에 카드 발급·해지 상태에 따른 대상 자격을 추가해야 합니다. [기존 캠페인 대상 검증](../../backend/core-service/src/modules/campaigns/services/campaigns.service.ts)

#### 예측 성능의 검증 범위

현재 후기 평가 기간은 이전 실험에도 사용한 자료이므로 **반복 시간순 평가 결과**로 설명합니다. 개발에 사용하지 않은 새 시기 자료를 확보하면 독립적인 성능 평가를 추가할 수 있습니다. 이미 확인한 자료를 다시 분할하는 것만으로 새로운 독립 테스트가 되지는 않습니다.

Synchrony의 실제·합성 데이터 여부와 생성 경위는 확인되지 않았으며 거래 범위는 해당 제휴몰입니다. 본 실험은 거래 기록에 누락이 없고 기록 당일에 정보를 이용할 수 있다고 가정합니다.

#### 실제 캠페인 효과를 평가하려는 경우

실제 캠페인의 해지 방지 효과까지 평가하려면 캠페인 적용 여부와 이후 결과를 별도로 수집하고, 적용군과 비교군을 어떻게 구성할지 정한 실험이 필요합니다. 현재 원본에는 해지 사유나 실제 캠페인 접촉·반응 기록이 없으므로, 해당 효과 검증은 **추가 데이터와 실험 설계가 필요한 별도 확장 과제**입니다.

Synchrony로 **기준일 이전 입력과 이후 해지 정답을 구분하는 오프라인 예측·평가 구조를 구현했습니다.** 서비스의 데이터·모델 전환과 월별 운영 자동화는 후속 개발 범위이며, 예측 성능은 현재 수행한 반복 시간순 평가의 범위에서 설명합니다.

## 관련 문서

- [프로젝트 메인 README](../../README.md)
- [Synchrony 실험 실행 방법](../../src/experiments/synchrony/README.md)
- [모델 개선 계획](../synchrony_improvement_plan.md)
- [추가 개선 결과와 신뢰구간](../synchrony_advanced_evaluation.md)
